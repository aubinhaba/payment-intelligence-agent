spring:
  cloud:
    aws:
      region:
        static: eu-west-1
      credentials:
        access-key: test
        secret-key: test
      dynamodb:
        endpoint: http://localhost:4566
      sqs:
        endpoint: http://localhost:4566
      s3:
        endpoint: http://localhost:4566
        path-style-access-enabled: true

# LocalStack SSM endpoint for SsmSecretsLoader
pia:
  aws:
    ssm:
      endpoint: http://localhost:4566

logging:
  level:
    com.aubin.pia: DEBUG
    io.awspring.cloud: INFO
